# QuizAppJava

Small JavaFX Quiz App (FXML + CSS), built with Maven.

## Requirements
- Java JDK 11+
- Maven 3+

## Build
From the repo root:

```bash
cd projects
mvn clean test
```

## Run
```bash
mvn -f projects/pom.xml javafx:run
```
